import { StyleSheet, View } from "react-native";

export default function Slider() {
  return <View style={styles.container}></View>;
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: "#3efd21",
  },
});
